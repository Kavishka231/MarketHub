import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{cancelOrder,fetchOrder,fetchOrders}from"../api/orders";export const orderKeys={all:["customer","orders"]as const,list:(page:number)=>["customer","orders","list",page]as const,detail:(id:number)=>["customer","orders","detail",id]as const};export function useOrders(page:number){return useQuery({queryKey:orderKeys.list(page),queryFn:()=>fetchOrders(page,10),placeholderData:p=>p})}export function useOrder(id:number){return useQuery({queryKey:orderKeys.detail(id),queryFn:()=>fetchOrder(id),enabled:Number.isInteger(id)&&id>0,retry:(count,error)=>{const status=(error as{response?:{status?:number}}).response?.status;return status===403||status===404?false:count<2}})}export function useCancelOrder(id:number){const c=useQueryClient();return useMutation({mutationFn:()=>cancelOrder(id),onSuccess:()=>{c.invalidateQueries({queryKey:orderKeys.all});c.invalidateQueries({queryKey:orderKeys.detail(id)})}})}
