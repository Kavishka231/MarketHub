import type{Product,ProductStatus}from"./marketplace";import type{OrderStatus,PaymentMethod}from"./shopping";import type{UserRole,UserStatus}from"./auth";export interface AdminUser{id:number;firstName:string;lastName:string;email:string;phone:string|null;role:UserRole;status:UserStatus;createdAt:string}export interface AdminUserPage{content:AdminUser[];page:number;size:number;totalElements:number;totalPages:number}export type VendorStatus="PENDING"|"APPROVED"|"REJECTED"|"SUSPENDED";export interface AdminVendor{id:number;userId:number;storeName:string;description:string|null;phone:string;status:VendorStatus;createdAt:string;updatedAt:string}export interface AdminProductPage{content:Product[];page:number;size:number;totalElements:number;totalPages:number}export interface AdminOrderSummary{id:number;orderNumber:string;status:OrderStatus;customerId:number;customerName:string;total:number;paymentMethod:PaymentMethod;createdAt:string}export interface AdminOrderPage{content:AdminOrderSummary[];page:number;size:number;totalElements:number;totalPages:number}export interface AdminOrderDetail extends AdminOrderSummary{subtotal:number;deliveryFee:number;customer:{id:number;name:string;email:string};delivery:{fullName:string;phone:string;addressLine1:string;addressLine2:string|null;city:string;district:string;postalCode:string|null};items:{id:number;productId:number;vendorId:number;vendorName:string|null;productName:string;unitPrice:number;quantity:number;subtotal:number;status:OrderStatus}[]}export interface AdminDashboard{totalUsers:number;totalCustomers:number;totalVendors:number;activeVendors:number;pendingVendors:number;suspendedVendors:number;totalProducts:number;activeProducts:number;outOfStockProducts:number;totalOrders:number;pendingOrders:number;deliveredOrders:number;totalSales:number}export type{ProductStatus,UserRole,UserStatus,OrderStatus};
