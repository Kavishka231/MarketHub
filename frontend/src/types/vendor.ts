import type{Category,Product,ProductStatus}from"./marketplace";import type{OrderStatus}from"./shopping";
export type VendorProduct=Product;export interface VendorProductPage{content:VendorProduct[];page:number;size:number;totalElements:number;totalPages:number}export interface ProductInput{name:string;description?:string;categoryId:number;price:number;stockQuantity:number;imageUrl?:string}export interface VendorProfile{id:number;userId:number;storeName:string;description:string|null;phone:string;status:"PENDING"|"APPROVED"|"REJECTED"|"SUSPENDED";createdAt:string;updatedAt:string}
export interface VendorOrderSummary{orderId:number;orderNumber:string;customerName:string;status:OrderStatus;vendorItemCount:number;vendorSubtotal:number;createdAt:string}export interface VendorOrderPage{content:VendorOrderSummary[];page:number;size:number;totalElements:number;totalPages:number}export interface VendorOrderItem{orderItemId:number;productId:number;productName:string;unitPrice:number;quantity:number;subtotal:number;status:OrderStatus}export interface VendorOrderDetail{orderId:number;orderNumber:string;orderStatus:OrderStatus;customerName:string;deliveryCity:string;deliveryDistrict:string;vendorItems:VendorOrderItem[];vendorSubtotal:number;createdAt:string}export type{Category,ProductStatus};
