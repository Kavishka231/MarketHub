import type{ProductSort as Sort}from "../../types/marketplace";export default function ProductSort({value,onChange}:{value:Sort;onChange:(v:Sort)=>void}){return <label className="text-sm">Sort<select aria-label="Sort products" className="ml-2 rounded border px-3 py-2" value={value} onChange={e=>onChange(e.target.value as Sort)}><option value="newest">Newest</option><option value="priceAsc">Price: low to high</option><option value="priceDesc">Price: high to low</option></select></label>}