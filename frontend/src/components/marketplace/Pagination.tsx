export default function Pagination({page,totalPages,onPage}:{page:number;totalPages:number;onPage:(page:number)=>void}){if(totalPages<=1)return null;return <nav aria-label="Product pagination" className="mt-8 flex items-center justify-center gap-4"><button className="rounded border px-4 py-2 disabled:opacity-40" disabled={page===0} onClick={()=>onPage(page-1)}>Previous</button><span>Page {page+1} of {totalPages}</span><button className="rounded border px-4 py-2 disabled:opacity-40" disabled={page+1>=totalPages} onClick={()=>onPage(page+1)}>Next</button></nav>}