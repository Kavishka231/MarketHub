import{Link}from"react-router-dom";import type{Cart}from"../../types/shopping";
export default function CartSummary({cart}:{cart:Cart}){return <aside className="h-fit rounded-xl border bg-white p-5"><h2 className="text-xl font-bold">Cart summary</h2><div className="mt-4 flex justify-between"><span>Items ({cart.totalItems})</span><strong>{new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(cart.subtotal)}</strong></div><p className="mt-3 text-xs text-slate-500">Final pricing, stock, and delivery fees are confirmed at checkout.</p><Link className="mt-5 block rounded bg-brand-700 px-4 py-3 text-center font-semibold text-white" to="/account/checkout">Proceed to checkout</Link></aside>}
