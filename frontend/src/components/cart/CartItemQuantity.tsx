export default function CartItemQuantity({value,max,disabled,onChange}:{value:number;max:number;disabled:boolean;onChange:(quantity:number)=>void}){return <div className="flex items-center gap-2"><button aria-label="Decrease quantity" className="rounded border px-3 py-1 disabled:opacity-40" disabled={disabled||value<=1} onClick={()=>onChange(value-1)}>−</button><input aria-label="Cart quantity" className="w-16 rounded border p-1 text-center" min="1" max={max} type="number" value={value} disabled={disabled} onChange={event=>{const next=Number(event.target.value);if(Number.isInteger(next)&&next>=1&&next<=max)onChange(next)}}/><button aria-label="Increase quantity" className="rounded border px-3 py-1 disabled:opacity-40" disabled={disabled||value>=max} onClick={()=>onChange(value+1)}>+</button></div>}
