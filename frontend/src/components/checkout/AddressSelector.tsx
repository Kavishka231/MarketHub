import type{Address}from"../../types/shopping";export default function AddressSelector({addresses,value,onChange}:{addresses:Address[];value?:number;onChange:(id:number)=>void}){return <fieldset className="space-y-3"><legend className="font-semibold">Saved delivery addresses</legend>{addresses.map(a=><label className={`block cursor-pointer rounded-lg border p-4 ${value===a.id?"border-brand-600 bg-brand-50":""}`} key={a.id}><input className="mr-3" type="radio" name="address" checked={value===a.id} onChange={()=>onChange(a.id)}/><strong>{a.fullName}</strong>{a.defaultAddress&&<span className="ml-2 text-xs text-brand-700">Default</span>}<span className="mt-1 block pl-6 text-sm text-slate-600">{a.addressLine1}{a.addressLine2?`, ${a.addressLine2}`:""}, {a.city}, {a.district}{a.postalCode?` ${a.postalCode}`:""} · {a.phone}</span></label>)}</fieldset>}
