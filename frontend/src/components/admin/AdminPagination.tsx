export default function AdminPagination({page,totalPages,onPage}:{page:number;totalPages:number;onPage:(page:number)=>void}){return <div className="mt-6 flex justify-between"><button className="rounded border px-4 py-2 disabled:opacity-40" disabled={page===0} onClick={()=>onPage(page-1)}>Previous</button><span>Page {page+1} of {Math.max(totalPages,1)}</span><button className="rounded border px-4 py-2 disabled:opacity-40" disabled={page+1>=totalPages} onClick={()=>onPage(page+1)}>Next</button></div>}
