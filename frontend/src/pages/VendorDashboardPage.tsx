import{Link}from"react-router-dom";import VendorStatCard from"../components/vendor/VendorStatCard";import{useVendorProducts}from"../hooks/useVendorProducts";import{useVendorOrders,useVendorProfile}from"../hooks/useVendorOrders";import{apiErrorMessage}from"../utils/apiError";const money=(n:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);export default function VendorDashboardPage(){const profile=useVendorProfile(),products=useVendorProducts(0),orders=useVendorOrders(undefined,0,5);if(profile.isLoading||products.isLoading||orders.isLoading)return <main className="mx-auto max-w-6xl px-6 py-10"><div className="h-56 animate-pulse rounded-xl bg-slate-200"/></main>;const error=profile.error??products.error??orders.error;if(error)return <main className="mx-auto max-w-4xl px-6 py-16 text-center"><h1 className="text-2xl font-bold">Dashboard unavailable</h1><p role="alert">{apiErrorMessage(error,"Could not load vendor dashboard")}</p></main>;return <main className="mx-auto max-w-6xl px-6 py-10"><h1 className="text-3xl font-bold">{profile.data!.storeName}</h1><p className="text-slate-600">Vendor dashboard · {profile.data!.status}</p><div className="mt-6 grid gap-4 sm:grid-cols-3"><VendorStatCard label="Products" value={products.data!.totalElements} detail="All products in your catalog"/><VendorStatCard label="Orders" value={orders.data!.totalElements} detail="Orders containing your products"/><VendorStatCard label="Recent page sales" value={money(orders.data!.content.reduce((sum,o)=>sum+o.vendorSubtotal,0))} detail="Vendor subtotal for the recent orders shown"/></div><section className="mt-8 rounded-xl border bg-white p-5"><div className="flex justify-between"><h2 className="text-xl font-bold">Recent orders</h2><Link className="text-brand-700 underline" to="/vendor/orders">View all</Link></div>{orders.data!.content.length?orders.data!.content.map(o=><Link className="mt-4 flex justify-between border-t pt-4 hover:text-brand-700" key={o.orderId} to={`/vendor/orders/${o.orderId}`}><span><strong>{o.orderNumber}</strong><small className="block text-slate-500">{o.customerName} · {o.status}</small></span><strong>{money(o.vendorSubtotal)}</strong></Link>):<p className="mt-4 text-slate-600">No vendor orders yet.</p>}</section></main>}
