import{Link,useParams}from"react-router-dom";import{AdminError,AdminLoading}from"../components/admin/AdminState";import{useAdminOrder}from"../hooks/useAdminOversight";import{normalizeApiError}from"../utils/apiError";const money=(n:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);export default function AdminOrderDetailPage(){const id=Number(useParams().orderId),q=useAdminOrder(id);if(q.isLoading)return <main className="mx-auto max-w-5xl px-6 py-10"><AdminLoading/></main>;if(q.isError){const e=normalizeApiError(q.error);return <main className="mx-auto max-w-3xl px-6 py-10"><AdminError message={e.status===404?"Order not found":e.message}/></main>}const o=q.data!;return <main className="mx-auto max-w-5xl px-6 py-10"><Link className="underline" to="/admin/orders">← Orders</Link><div className="mt-6 flex justify-between"><div><h1 className="text-3xl font-bold">Order {o.orderNumber}</h1><p>{o.customer.name} · {o.customer.email}</p></div><strong>{o.status}</strong></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_320px]"><section className="rounded-xl border bg-white p-5"><h2 className="text-xl font-bold">Items</h2>{o.items.map(i=><div className="border-b py-4" key={i.id}><div className="flex justify-between"><strong>{i.productName}</strong><strong>{money(i.subtotal)}</strong></div><p className="text-sm text-slate-500">{i.vendorName??`Vendor ${i.vendorId}`} · {i.quantity} × {money(i.unitPrice)} · {i.status}</p></div>)}</section><aside className="space-y-4"><section className="rounded-xl border bg-white p-5"><h2 className="font-bold">Delivery</h2><p className="mt-2 text-sm">{o.delivery.fullName}<br/>{o.delivery.addressLine1}<br/>{o.delivery.city}, {o.delivery.district}<br/>{o.delivery.phone}</p></section><section className="rounded-xl border bg-white p-5"><p className="flex justify-between"><span>Subtotal</span><span>{money(o.subtotal)}</span></p><p className="flex justify-between"><span>Delivery</span><span>{money(o.deliveryFee)}</span></p><p className="mt-2 flex justify-between border-t pt-2 font-bold"><span>Total</span><span>{money(o.total)}</span></p><p className="mt-2 text-xs">{o.paymentMethod.replaceAll("_"," ")}</p></section></aside></div></main>}
