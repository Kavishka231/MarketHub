import{Link,useParams}from"react-router-dom";import{AdminError,AdminLoading}from"../components/admin/AdminState";import{useAdminUser,useSetUserEnabled}from"../hooks/useAdminGovernance";import{normalizeApiError,apiErrorMessage}from"../utils/apiError";export default function AdminUserDetailPage(){const id=Number(useParams().userId),q=useAdminUser(id),mutation=useSetUserEnabled(id);if(q.isLoading)return <main className="mx-auto max-w-3xl px-6 py-10"><AdminLoading/></main>;if(q.isError){const e=normalizeApiError(q.error);return <main className="mx-auto max-w-3xl px-6 py-10"><AdminError message={e.status===404?"User not found":e.message}/></main>}const u=q.data!,enable=u.status==="DISABLED";return <main className="mx-auto max-w-3xl px-6 py-10"><Link className="underline" to="/admin/users">← Users</Link><section className="mt-6 rounded-xl border bg-white p-6"><h1 className="text-3xl font-bold">{u.firstName} {u.lastName}</h1><dl className="mt-5 grid gap-3 sm:grid-cols-2"><div><dt>Email</dt><dd>{u.email}</dd></div><div><dt>Phone</dt><dd>{u.phone??"Not provided"}</dd></div><div><dt>Role</dt><dd>{u.role}</dd></div><div><dt>Status</dt><dd>{u.status}</dd></div></dl>{mutation.isError&&<p role="alert" className="mt-4 text-red-700">{apiErrorMessage(mutation.error,"Account update failed")}</p>}<button className="mt-6 rounded border px-4 py-2" disabled={mutation.isPending} onClick={()=>{if(enable||window.confirm("Disable this account?"))mutation.mutate(enable)}}>{mutation.isPending?"Updating…":enable?"Enable account":"Disable account"}</button></section></main>}
