import{render,screen}from "@testing-library/react";import{MemoryRouter}from "react-router-dom";import{AuthProvider}from "./features/auth/AuthContext";import App from "./App";describe("application routes",()=>{it("renders the home route",()=>{render(<MemoryRouter><AuthProvider><App/></AuthProvider></MemoryRouter>);expect(screen.getByRole("heading",{name:/shop from trusted/i})).toBeInTheDocument()})});