import client from"./client";import type{OrderDetail,OrderPage,OrderSummary}from"../types/shopping";export async function fetchOrders(page=0,size=10):Promise<OrderPage>{return(await client.get<OrderPage>("/api/orders",{params:{page,size}})).data}export async function fetchOrder(id:number):Promise<OrderDetail>{return(await client.get<OrderDetail>(`/api/orders/${id}`)).data}export async function cancelOrder(id:number):Promise<OrderSummary>{return(await client.patch<OrderSummary>(`/api/orders/${id}/cancel`)).data}
